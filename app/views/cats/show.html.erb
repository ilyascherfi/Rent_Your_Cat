<div class = "container">

<div class = "row col-6">
  <h1> <%= @cat.name %> </h1>
    <ul>
        <li class = "well"> <%= @cat.race %></li>
        <li class = "well"> <%= @cat.sexe %></li>
        <li class = "well"><%= @cat.description %></li>
        <li class = "well"> <%= @cat.location %></li>
    </ul>
  </div>

  <div class = "d-flex justify-content-between">
      <div>
              <%= link_to "Supprimer le chat", cat_path(@cat), class: "btn btn-danger btn btn-cancel rounded-pill", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this cat" } %>
      </div>

      <div>
              <%= link_to "Mettre à jour ses informations", edit_cat_path(@cat), class: "btn btn-primary btn btn-cancel rounded-pill" %>
      </div>
    </div>
  </div>

<%# <%= link_to "Supprimer le chat", cat_path(@cat), class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this cat" } %>


<%# edit_cat

<%# delete cat %>
