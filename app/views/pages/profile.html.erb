<div class="container center">
  <ul>
    <li>
      <img class='avatar-large' src="<%= current_user.image_url %>" alt="">
    </li>
    <br>
    <li>
      prenom : <%= current_user.first_name %>
    </li>
      <br>
    <li>
      nom : <%= current_user.last_name %>
    </li>
      <br>
    <li>
      email : <%= current_user.email %>
    </li>
      <br>
    <li>
      address : <%= current_user.address %>
    </li>
      <br>
  </ul>
  <%= link_to 'edit', edit_user_registration_path %>
</div>

<div class = "card">
  <%= current_user.cats.first.name %>
  <% current_user.cats.first.photos.each do |photo| %>
    <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
  <% end %>
</div>

<%# <%= cl_image_tag cats.key, height: 300, width: 400, crop: :fill %>
