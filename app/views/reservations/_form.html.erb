<div class= " pt-5">
    <%= simple_form_for [@cat, @reservation] do |f| %>
  <div class = "fs-5" >
      <%= f.input :start_date,
                  as: :string,
                  input_html: { data: { controller: "datepicker" } } %>
      <%= f.input :end_date,
                  as: :string,
                  input_html: { data: { controller: "datepicker" } } %>
  </div>
  <div class="d-flex  flex-row-reverse fs-3">
        <%= f.submit "Reserver le Chat", class:"btn-grad" %>
  </div>
    <%end%>
</div>
