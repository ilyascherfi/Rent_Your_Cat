<div class= " pt-5">
  <div class = "fs-5" >
    <%= simple_form_for [@cat, @reservation] do |f| %>
      <%= f.input :start_date,
      as: :string,
      input_html: { data: { controller: "datepicker" } } %>
        <%= f.input :end_date,
      as: :string,
      input_html: { data: { controller: "datepicker" } } %>
  </div>
  <div class="d-flex  flex-row-reverse fs-3">
      <%= f.button :submit, "Reserver le Chat", class:"btn-grad" %>
    <%end%>
  </div>
</div>
